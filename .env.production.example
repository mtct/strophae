# Production template. Copy to .env.production, fill in the real values, and
# never commit that file (.gitignore already excludes it). Run Django with
# DJANGO_ENV=production to load it. Variables injected directly by the
# deployment platform take precedence over this file.
POSTGRES_DB=strophae
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=5432
